export const getTitle = state => state.notes.title;
export const getBody = state => state.notes.body;
export const getError = state => state.notes.error;
export const isLoading = state => state.notes.loading;
